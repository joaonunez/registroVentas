document.addEventListener('DOMContentLoaded', function () { // evento que se ejecuta cuando el DOM se ha cargado completamente
    actualizarTablaMeseros(); // llama a la funcion para actualizar la tabla de meseros
    actualizarTablaProductos(); // llama a la funcion para actualizar la tabla de productos
    actualizarTablaCategorias(); // llama a la funcion para actualizar la tabla de categorias
    mostrarVentas(); // llama a la funcion para mostrar las ventas
});
